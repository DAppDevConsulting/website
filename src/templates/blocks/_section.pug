mixin section(title, subtitle, classes_mod, section_id)
		+b('section').section(class=classes_mod id=section_id)
			.row
				if title
					+e.heading
						+e('h2').title= title
						if subtitle
							+e('p').subtitle= subtitle
				if block
					+e.content
						block
				else
					p No content provided

mixin page-nav(items, classes_mod)
	+b('aside').page-nav(class=classes_mod id='pageNav')
		+e('ul').list.nav
			each item in items
				+e('li').list-item
					+e('a').link.nav-link(href=`${item.href}`) #{item.name}
